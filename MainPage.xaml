<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:TodoApp.ViewModels"
             x:Class="TodoApp.MainPage">

    <ContentPage.BindingContext>
        <viewmodels:MainViewModel />
    </ContentPage.BindingContext>

    <VerticalStackLayout Padding="20">

        <Entry Placeholder="Enter new task..." Text="{Binding NewTodoText}" />
        <Button Text="Add Task" Command="{Binding AddTodoCommand}" />

        <CollectionView ItemsSource="{Binding Todos}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border Stroke="Gray" StrokeShape="RoundRectangle 10" Margin="5" Padding="10">
                        <Grid ColumnDefinitions="*,Auto,Auto">
                            <CheckBox IsChecked="{Binding IsCompleted}"
                                      CheckedChanged="CheckBox_CheckedChanged"
                                      VerticalOptions="Center" />
                            <Label Text="{Binding Title}"
                                   Grid.Column="0"
                                   VerticalOptions="Center"
                                   Margin="10,0" />

                            <Button Text="✏️"
                                    Grid.Column="1"
                                    Margin="5,0"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:MainViewModel}}, Path=EditTodoCommand}"
                                    CommandParameter="{Binding .}" />

                            <Button Text="🗑️"
                                    Grid.Column="2"
                                    Margin="5,0"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:MainViewModel}}, Path=DeleteTodoCommand}"
                                    CommandParameter="{Binding .}" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </VerticalStackLayout>
</ContentPage>
